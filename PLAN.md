# 냥스택 (NYANG STACK) - 개발 계획서

## Context
v4: 상표 리스크로 이름을 "냥스택 (NYANG STACK)"으로 변경했다. The Tetris Company는 "-tris" 접미사(Tris, Setris)까지
삭제 요청을 내 왔고 Xio(Mino) 판결에서는 look and feel까지 인정됐으므로 이름과 문구에서 해당 단어를 완전히 뺐다.
localStorage 키 접두사 `cattetris_`는 기존 기록 호환을 위해 유지한다.

고양이 테마의 모바일 전용 블록 퍼즐(구 CAT TETRIS). React + TypeScript + CSS Modules, Vite.
v3에서 게임 엔진을 표준 가이드라인 수준으로 표준화하고, 고양이 뭉치 폭발 메커니즘을
핵심 점수원으로 재설계했으며, 코드 구조를 FSD(Feature-Sliced Design)로 재편했다.

---

## 기술 스택
| 항목 | 선택 |
|------|------|
| 프레임워크 | React 19 + TypeScript 5.9 |
| 스타일링 | CSS Modules (`.module.css`), 상대 단위(rem, %, dvh)만 사용 |
| 빌드 | Vite 8 (`npm run build`), 단일 파일 번들 `npm run build:single` |
| 테스트 | vitest (`npm test`) - 순수 게임 로직 34개 |
| 린트 | ESLint + steiger(FSD 레이어 검사) (`npm run lint`) |
| 밸런스 | 봇 시뮬레이션 `npm run sim`, `npm run sim:sweep` |
| 아이콘 | 인라인 SVG (폰트 로딩 실패에 영향받지 않음) |
| 폰트 | Plus Jakarta Sans, Be Vietnam Pro (Google Fonts, 시스템 폰트 폴백) |

---

## 프로젝트 구조 (FSD)
레이어는 위에서 아래로만 import 한다. 같은 레이어의 다른 슬라이스는 `@x/<대상슬라이스>.ts` 공개 API로만 참조한다.

```
src/
├── app/                         진입점, 전역 스타일
│   ├── main.tsx / App.tsx
│   └── styles/global.css        디자인 토큰, 테마(data-theme) 변수
├── pages/game/                  위젯을 조합하는 단일 페이지 (GamePage)
├── pages/game/model/            useSessionRecorder (판 종료 시 미션/통계 기록)
├── widgets/
│   ├── header/                  타이틀, 도감/통계/홈/설정, 타이머+일시정지 (오버레이 위에 표시)
│   ├── stats-hud/               점수·최고·레벨·줄·콤보 한 줄 (무한 모드)
│   ├── stage-goals/             스테이지 목표 진행과 남은 조각/시간 (스테이지 모드)
│   ├── hold-slot/               홀드 슬롯(탭하면 홀드)
│   ├── next-queue/              다음 조각 3개
│   ├── board/                   10x20 격자, 제거 연출, 캔버스 파티클(ExplosionLayer), 점수 팝업, 표정 규칙(model/expression)
│   ├── controls/                홀드/좌/소프트드롭/우/회전 버튼 (DAS 170ms, ARR 50ms)
│   ├── game-overlays/           시작(모드 탭, 난이도, 스테이지 카드, 오늘의 미션), 일시정지, 게임오버, 클리어
│   ├── stage-select/            스테이지 목록 (잠금/별)
│   ├── settings-overlay/        설정 시트 (튜토리얼 다시 보기 포함)
│   ├── tutorial-overlay/        코치 카드 (점수판 자리)
│   ├── stats-overlay/           누적 통계, 난이도별 최고, 칭호
│   └── collection/              고양이 도감 + 스킨 스와치 + 액세서리
├── features/
│   ├── game-session/            순수 리듀서 엔진(engine.ts) + rAF 루프 훅(useGame.ts), 스테이지 별 저장
│   ├── keyboard-controls/       키보드 매핑
│   ├── touch-gestures/          보드 위 끌기/플릭/탭 제스처
│   ├── settings/                설정 저장/병합/테마 적용
│   ├── sound/                   Web Audio 합성기, 효과음 레시피, 칩튠 루프
│   ├── daily-missions/          오늘의 미션 진행/수령, 칭호
│   ├── player-stats/            누적 통계
│   ├── tutorial/                7단계 튜토리얼 단계 정의/리듀서/훅
│   └── skins/                   스킨 장착 저장과 해금 검증
├── entities/
│   ├── cat/                     CatType 12종, 품종 정보, 털 색, 스킨/액세서리 정의와 해금 규칙, SkinProvider, CatBlock UI(표정·스킨)
│   ├── piece/                   테트로미노 정의, SRS 킥 테이블, 7-bag, 품종 추첨, PiecePreview
│   ├── board/                   격자 연산(충돌, 배치, 줄, 뭉치 탐색, 폭발 여파, 중력)
│   ├── difficulty/              난이도 프리셋, 낙하 속도 공식, 품종 수 계산
│   ├── stage/                   스테이지 30개 정의, 목표/제한 판정, 별점
│   └── mission/                 날짜 시드 미션 생성, 진행 판정
└── shared/
    ├── config/                  보드 크기, 점수표, 연출 시간, localStorage 키, 테마 id
    ├── lib/                     시드 난수(mulberry32), 안전한 storage 유틸
    └── ui/                      Icon(SVG)
public/
├── fonts/pretendard/            Pretendard Variable 다이내믹 서브셋(92개) + 전체 가변 폰트(아티팩트용)
└── icons/                       PWA 아이콘 (192/512/maskable/apple-touch)
scripts/
├── balance-sim.ts               난이도별 봇 플레이 지표
└── balance-sweep.ts             폭발 기준/품종 수/반복 확률 스윕
```

---

## 시중 블록 퍼즐(가이드라인)과의 비교 (v2 -> v3)
| 항목 | 가이드라인 표준 | v2 (이전) | v3 (현재) |
|------|-------------------|-----------|-----------|
| 회전 | SRS + 벽 킥 | 수평 킥만 | SRS 킥 테이블 (I/JLSTZ/O) |
| 랜덤 | 7-bag | 순수 랜덤 | 7-bag (모양), 품종은 난이도별 풀에서 추첨 |
| 락 딜레이 | 0.5s, 이동 리셋 15회 | 없음(즉시 고정) | 난이도별 400~600ms, 리셋 10~15회, 더 낮은 줄 도달 시 초기화 |
| 홀드 | 있음 | 없음 | 있음 (조각당 1회) |
| 다음 미리보기 | 1~6개 | 1개 | 3개 |
| 낙하 속도 | (0.8-(L-1)·0.007)^(L-1) 초 | 10단계 고정표, 레벨 10 이후 정체 | 가이드라인 공식, 난이도별 상한(8/10/13) |
| 점수 | 100/300/500/800 × 레벨, 드롭 점수 | 레벨 배율 없음, 소프트 드롭 0점 | 줄 × 레벨 × 난이도 배율, 소프트 1/칸, 하드 2/칸, 콤보 보너스 |
| 고스트 | 있음 | 있음 | 있음 (토글) |
| 게임오버 | block out / lock out | 스폰 불가만 | block out + lock out |
| 조작(모바일) | 버튼 + 제스처 | 버튼 4개, 단일 반복 속도 | 버튼 5개(DAS/ARR 분리) + 보드 제스처 |
| 조작(키보드) | ←→↓, Space 하드, ↑/X, Z, C, P | Space가 회전 | 가이드라인 배치 |
| 연출/피드백 | 줄 삭제 애니메이션, 효과음 | 없음(동기 루프) | 제거/폭발/여파 keyframe, 캔버스 파티클, 보드 흔들림, 팝업, 진동 |
| 레벨 표시 | 있음 | 계산만 함 | HUD 표시 + 레벨업 팝업 |
| T-spin, B2B | 있음 | 없음 | 없음 (폭발 연쇄가 그 역할, 의도적으로 제외) |

---

## 게임 규칙 (엔진)
`features/game-session/model/engine.ts`는 상태와 액션만 받는 순수 리듀서다. 시간은 `tick(dt)`로 들어오며
훅이 requestAnimationFrame으로 공급한다. 시드 난수를 상태에 들고 다니므로 같은 시드는 같은 판을 만든다.

### 조각 흐름
1. `start`: 품종 순서를 시드로 섞고 큐 3개를 채운 뒤 첫 조각을 낸다.
2. 낙하: 레벨/난이도로 계산한 간격마다 1칸. 바닥에 닿으면 락 딜레이 타이머 시작.
3. 이동/회전 성공 시 타이머 리셋(횟수 제한). 더 낮은 줄에 도달하면 횟수 초기화.
4. 고정 -> **판정** -> (있으면) **제거 연출 320ms** -> 셀 제거 + 열 단위 중력 -> **정착 140ms** -> 다시 판정(연쇄 +1) -> 없으면 다음 조각.

### 판정 한 단계에서 지워지는 것
- 완성된 줄의 모든 셀 (`line`)
- 같은 품종이 4방향으로 **기준 개수 이상** 이어진 뭉치 (`cluster`)
- 뭉치 각 셀의 **8방향 인접 1칸**에 있는 다른 블록, 품종 무관 (`splash`)

### 점수
- 줄: 100 / 300 / 500 / 800 (5줄 이상은 800 + 400/줄)
- 뭉치: 뭉치 셀 × 30 + 여파 셀 × 10
- 위 합계 × 레벨 × (1 + 0.5 × 연쇄 단계) × 난이도 배율
- 콤보(연속으로 무언가를 지운 조각 수) 2 이상: 50 × (콤보-1) × 레벨 × 배율
- 소프트 드롭 1점/칸, 하드 드롭 2점/칸

---

## 난이도 (entities/difficulty)
v4에서 격차를 한 번 더 벌렸다. '상'은 가이드라인 20G에 해당하는 의도적 극한 난이도다.

| 변수 | 하 | 중 | 상 |
|------|----|----|----|
| 시작 레벨 (시작 낙하 간격) | 1 (1000ms) | 5 (355ms) | 10 (64ms) |
| 속도 상한 레벨 (상한 낙하 간격) | 10 (64ms) | 13 (19ms) | 15 (7ms, 스폰 즉시 바닥) |
| 락 딜레이 / 리셋 횟수 | 500ms / 15 | 400ms / 10 | 300ms / 5 |
| 등장 품종 수 (시작 -> 최대, 4레벨마다 +1) | 5 -> 6 | 6 -> 7 | 8 -> 10 |
| 직전 품종 반복 확률 | 0.10 | 0.10 | 0 |
| 폭발 기준 (같은 품종 연결 수) | 10 | 12 | 14 |
| 점수 배율 | 1.0 | 1.5 | 3.0 |

최고 기록은 난이도별로 따로 저장한다 (`cattetris_highscore_<id>`). 예전 단일 키 기록은 '중'으로 승계.

### 시뮬레이션 근거 (`npm run sim`, 12판 × 최대 400조각, 낙하 시간은 고려하지 않는 배치 봇)
| 봇 | 난이도 | 버틴 조각 | 줄/100조각 | 폭발/100조각 | 게임오버 |
|----|--------|----------|-----------|-------------|---------|
| 무작위 배치 | 하 | 30 | 1.7 | 5.5 | 100% |
| 무작위 배치 | 중 | 26 | 0.7 | 4.2 | 100% |
| 무작위 배치 | 상 | 22 | 1.2 | 1.2 | 100% |
| 휴리스틱(높이·구멍 최소화, 같은 품종 선호) | 하 | 400 | 21.6 | 10.9 | 0% |
| 휴리스틱 | 중 | 400 | 26.4 | 6.9 | 0% |
| 휴리스틱 | 상 | 342 | 35.1 | 1.3 | 25% |

배치 봇은 낙하 속도를 무시하므로 실제 체감 격차는 속도 차이(시작 1000 -> 355 -> 64ms, 상한 64 -> 19 -> 7ms)만큼 훨씬 크다.
'상'은 레벨 15부터 스폰 즉시 바닥에 닿아 락 딜레이 300ms 안에 이동·회전 5회로만 조작해야 한다.
첫 안(5/6/7마리)은 조각 100개당 폭발 30~40회로 줄 삭제가 거의 사라져 폐기했다.
스윕 결과 전체는 `npm run sim:sweep`으로 재현할 수 있다.

---

## 고양이 품종 (12종)
치즈냥이, 턱시도, 러시안블루, 삼색이, 샴, 까망이, 고등어, 실버태비 (기존 8종)
흰냥이 오드아이(눈 색이 다름), 카오스(거북등), 벵갈(로제트), 스코티시 폴드(접힌 귀) (v3 추가)

- 무늬는 전부 CSS(`entities/cat/ui/CatBlock.module.css`)이며 `background-attachment: fixed`로 인접 블록과 이어진다.
- 얼굴/꼬리는 연결된 같은 품종 그룹당 하나씩(위-왼쪽 / 아래-오른쪽).
- 도감은 품종별 누적 제거 500마리에 해금. 통계는 제거가 일어날 때마다 localStorage에 누적.

---

## 조작
| 입력 | 동작 |
|------|------|
| 버튼 | 홀드 · 왼쪽 · 소프트 드롭 · 오른쪽 · 회전 (좌우/아래는 DAS 170ms 후 ARR 50~60ms 반복) |
| 보드 끌기 | 좌우: 셀 단위 이동, 아래로 천천히: 소프트 드롭 |
| 보드 플릭(아래) | 하드 드롭 |
| 보드 탭 | 시계 방향 회전 |
| 키보드 | ← → ↓, Space 하드 드롭, ↑/X 회전, Z/Ctrl 반시계, C/Shift 홀드, P/Esc 일시정지, Enter 시작 |
| 화면 이탈 | 자동 일시정지 (배경음 정지) |
| 플레이 중 홈 버튼 | 바로 나가지 않고 일시정지 화면으로 |

---

## 검증 방법
1. `npm test` - 엔진/엔티티 유닛 테스트 (SRS 킥, 7-bag, 락 딜레이, 홀드, 줄 삭제, 폭발+여파+연쇄, 게임오버, 일시정지, 스테이지 목표/제한, 미션 생성, 설정 병합, 표정 규칙, 통계 누적)
2. `npm run lint` - ESLint + steiger(FSD 레이어 위반)
3. `npm run build` - 타입 검사 + 번들
4. `npm run sim` - 난이도 지표가 위 표 근처인지 확인
5. `npm run dev` 수동 확인: 난이도 선택 -> 플레이 -> 같은 품종 뭉치 폭발과 주변 여파 -> 연쇄 팝업 -> 게임오버 신기록 -> 도감
6. 스테이지 탭 -> 스테이지 1 클리어 -> 별 화면 -> 다음 스테이지, 설정 시트 토글 반영, 오늘의 미션 받기 -> 칭호 표시, 통계 화면
8. 첫 실행 카드 -> 튜토리얼 7단계 완주(옮기기/돌리기/내려놓기/홀드/줄/팡/안내) -> 완료 저장, 설정에서 다시 보기
9. 도감에서 카드 탭 -> 스와치 선택 -> 보드/미리보기/행렬 색 변경, 액세서리 장착 -> 얼굴 셀에 표시, 폭발 파티클 색이 장착 팔레트를 따름
7. 실기기: iOS Safari 첫 터치 후 효과음, 홈 화면 추가(PWA) 후 오프라인 실행, 폰트가 네트워크 없이도 동일

---

## v4에서 완료한 항목 (상업성 평가 반영, 수익 모델 제외)
| 단계 | 내용 |
|------|------|
| 0 | 난이도 격차 확대: 하 L1/상한10, 중 L5/상한13, 상 L10/상한15(20G)·락 300ms/5회·품종 8~10·14마리 |
| A | 이름 변경 냥스택(NYANG STACK). "-tris" 접미사 이름(Tris, Setris) 삭제 사례 때문에 CATRIS 등은 배제 |
| F | 설정 시트: 효과음/배경음/진동/제스처/고스트/기본 난이도/테마, 예전 키 승계 |
| B | Web Audio 합성 효과음 13종(연쇄 단계마다 음정 상승)과 칩튠 배경음(레벨에 따라 템포 상승), iOS 잠금 해제 |
| C | 표정: 깜빡임(셀별 지연), 기쁨(줄/여파), 어지러움(폭발 뭉치), 겁먹음(스택 14줄 이상), 잠(게임오버) |
| D | 스테이지 30개: 줄/폭발/품종/연쇄/점수 목표, 조각/시간 제한, 별점(남은 제한 30%/60%), 진행 저장과 해금 |
| E | 오늘의 미션 3개(날짜 시드, 칭호 보상), 누적 통계 화면(판수, 시간, 줄, 폭발, 최대 연쇄, 난이도별 최고) |
| G | PWA(manifest, 서비스 워커 프리캐시, 아이콘), 폰트 자체 호스팅 |

## v4.1에서 완료한 항목
| 항목 | 내용 |
|------|------|
| 튜토리얼 | 실제 보드에서 7단계(옮기기, 돌리기, 내려놓기, 홀드, 줄 지우기, 팡, 안내). 엔진 `start.setup`으로 보드/조각 순서를 미리 깔고, 엔진 이벤트(move/rotate/hardDrop/hold/line/explode)로 단계 완료 판정. 첫 실행 카드, 시작 화면 ? 버튼, 설정 "다시 보기". 코치 카드는 점수판 자리에 들어가고 대상 버튼/보드에 펄스 링(`body[data-coach]`) |
| 버그 수정 | 판을 다시 시작하면 피드백 이벤트 번호가 0부터 시작해 효과음/진동이 누락되던 문제 |
| 스킨 | 품종별 대체 팔레트 2종(24종, 누적 500/2,000마리 해금)과 액세서리 4종(리본/방울/안경/왕관, 스테이지 별 10/30/60/90). CatBlock 색을 CSS 변수로 통일하고 `SkinProvider` 컨텍스트로 보드·미리보기·행렬·파티클까지 반영. 도감 카드를 탭하면 스와치가 펼쳐지고 즉시 장착 |

## v5 비주얼 리디자인 "젤리 로프" (완료)
### 방향과 차별점
시중 블록 퍼즐과 비교해 세 가지 축으로 차별화했다.
- **캐릭터가 조각이다**: 조각 하나가 둥근 젤리 덩어리 고양이(광택 콩알눈, 볼 터치, 코, 수염, ω 입)이고 깜빡이고 귀를 쫑긋하며 꼬리를 흔든다. 얼굴은 조각의 머리 셀, 꼬리는 끝 셀에 붙어 회전해도 한 마리로 읽힌다.
- **젤리 물성**: 조각이 미끄러지듯 움직이고(플로팅 레이어 트랜지션), 착지하면 눌렸다 튀어오르며 먼지가 인다. 스폰은 팝, 홀드는 플립, 다음 큐는 슬라이드. 폭발은 캔버스 파티클(털 조각·불꽃·충격파·발자국)과 보드 흔들림.
- **유려한 파스텔 UI**: 크림 바탕에 복숭아·라벤더·민트 블롭이 천천히 떠다니고, 헤더·점수판·컨트롤 독은 유리 질감, 액센트는 코랄→핑크→라벤더 그라데이션. 고스트는 점선 대신 품종 색의 그림자 쿠션.

### 참고한 사례
- Tetris Effect: 파티클을 기본 재료로 삼아 음악에 반응하는 연출 (우리는 폭발과 착지를 파티클로)
- Suika Game: 귀여운 얼굴 + 물리감 + 부드러운 효과음이 만드는 중독 루프 (우리는 젤리 스쿼시·먼지·표정)
- Puyo Puyo Tetris: 캐릭터가 이끄는 퍼즐 (우리는 조각 자체가 캐릭터)

### 사용자에게 버그로 보일 수 있어 제거한 현상
| 현상 | 원인 | 조치 |
|------|------|------|
| 블록이 움직일 때 무늬가 제자리에 고정되어 미끄러져 보임 | `background-attachment: fixed` | 셀 2x2 타일 + 셀 위상(--px/--py)으로 무늬가 조각과 함께 이동, 인접 셀은 이어짐 |
| 꼬리가 보드 가장자리/옆 셀에서 잘림 | 셀 밖으로 삐져나오는 테두리 꼬리 | 셀 안 SVG 곡선 꼬리 |
| 점선 고스트가 연결부에서 끊겨 깨져 보임 | 테두리 제거 규칙 | 반투명 그림자 쿠션 |
| 보드 위 떠 있는 회색 막대 | 선반 장식 | 제거 |
| 대기/게임오버 화면의 00:00 타이머, 항상 보이는 조작 힌트 | 상태 무관 렌더 | 플레이 중에만 타이머, 힌트는 튜토리얼로 대체 |
| 연결 상태가 바뀔 때 모든 속성이 애니메이션되며 번쩍임 | `transition: all` | 모서리/그림자만 전환 |
| 재시작 후 효과음·진동 누락 | 이벤트 번호 초기화 | 판 사이에 번호 유지 (v4.1) |

## 모바일 뷰포트 고정과 폰트 (v5.1)
- `html, body { position: fixed; inset: 0; overflow: hidden; overscroll-behavior: none }`, `#root { height: var(--app-height, 100dvh) }`.
  `app/model/useViewportHeight`가 `visualViewport.height`를 `--app-height`로 써서 주소창·툴바가 오르내려도 정확히 채운다.
- `app/lib/lockScroll`: `[data-scroll]` 안에서 실제로 더 스크롤할 수 있을 때만 터치/휠을 통과, 그 외엔 preventDefault (iOS 바운스·툴바 접힘 제스처 차단). 스크롤이 생기면 즉시 0으로 되돌린다.
- 내부 스크롤 영역(시작/게임오버 오버레이, 도감, 스테이지 목록, 설정, 통계)에만 `data-scroll` + `overscroll-behavior: contain`.
- `viewport-fit=cover` + safe-area-inset 반영, `max-height` 720/640/600px 단계로 압축 레이아웃(행렬·태그라인·부제·시작 화면 도감 버튼 순으로 숨김).
- 폰트: Pretendard Variable(OFL) 다이내믹 서브셋 92개를 자체 호스팅. 첫 로드에 전부 받지 않도록 프리캐시에서 제외하고 런타임 CacheFirst. 아티팩트 빌드에는 전체 가변 폰트를 base64로 인라인.
- 검증: 390x664, 375x553, 412x915에서 `scrollHeight === innerHeight`, `#root` 높이 === `visualViewport.height`, 휠/탭 후 `scrollY 0`, `document.fonts.check('Pretendard Variable')` true.

## 남은 개선 항목
| 우선순위 | 항목 | 상태 |
|---------|------|------|
| P1 | 수익 모델 (리워드 광고 이어하기, 스킨 판매, 광고 제거) | 보류 (사용자 지시) |
| P2 | 배경음 곡 추가와 스테이지별 분위기 변주 | 미완료 |
| P3 | T-spin / Back-to-Back 보너스 | 보류 (폭발 연쇄와 역할이 겹쳐 의도적으로 제외) |
| P3 | 온라인 랭킹 | 보류 |

### v3에서 완료한 항목
- FSD 구조 이동, `@/` 별칭, 슬라이스 공개 API, steiger 린트, vitest
- SRS 회전, 7-bag, 락 딜레이, 홀드, 다음 3개, 가이드라인 속도, 점수 체계, 레벨/줄 표시
- 고양이 뭉치 폭발 + 8방향 여파 + 단계별 연쇄 연출 + 팝업 + 진동
- 캔버스 파티클 폭발(털 조각, 불꽃, 충격파 링, 섬광, 발자국)과 보드 흔들림
- 난이도 상/중/하 (시뮬레이션 기반 조정), 난이도별 최고 기록
- 품종 4종 추가(12종), 도감 설명과 해금 기준 500, 도감 스크롤 버그 수정
- 컨트롤 DAS/ARR, 홀드 버튼, 보드 제스처, 가이드라인 키보드 배치
- 인라인 SVG 아이콘, 시작 화면 고양이 행렬, 신기록 표시와 게임 요약, 렌더 메모이제이션

---

## 커밋 컨벤션
- `FEAT:` 기능, `FIX:` 수정, `DOCS:` 문서, `REFACTOR:` 구조 변경, `CHORE:` 도구
- 각 기능/수정은 개별 커밋, PLAN.md 동반 갱신
